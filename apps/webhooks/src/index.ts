// @platform/webhooks
//
// Placeholder entry point created during project scaffolding (M0).
// No webhook endpoints, signature verification, or event processing
// exist yet.
//
// This is a dedicated, independently deployable service so that inbound
// webhook traffic (Vapi, Stripe, OAuth callbacks) is never at risk from a
// slower or buggier deploy of apps/api. Implementation begins with the
// milestone that introduces the first Vapi call loop.

export {};
