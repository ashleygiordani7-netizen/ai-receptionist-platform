{
  "name": "@platform/workers",
  "version": "0.0.0",
  "private": true,
  "description": "Placeholder service created during project scaffolding (M0). No endpoints, jobs, or business logic exist yet.",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "echo \"(placeholder) @platform/workers has no runnable entry point yet\"",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {}
}
