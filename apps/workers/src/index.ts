// @platform/workers
//
// Placeholder entry point created during project scaffolding (M0).
// No queue consumers or job processors exist yet.
//
// This app hosts all background job processing (transcript processing,
// call scoring, knowledge base embedding, analytics rollups, notification
// dispatch, provider synchronization). Implementation begins with the
// milestone that introduces the first genuinely async workflow.

export {};
