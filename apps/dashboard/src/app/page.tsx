// Placeholder page created during project scaffolding (M1).
// No real UI, data fetching, or business logic exists yet.
// This exists only so the app builds and runs as a valid shell.
export default function HomePage() {
  return (
    <main>
      <p>AI Receptionist Platform — project shell. No features implemented yet.</p>
    </main>
  );
}
