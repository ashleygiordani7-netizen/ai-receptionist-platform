// @platform/admin
//
// Placeholder entry point created during project scaffolding (M0).
// No server, routes, pages, or business logic exist yet.
//
// This app is the internal ops/superadmin console (organization
// provisioning, system health, support tools), separate from the
// tenant-facing apps/dashboard. Per the Milestone Roadmap, its first real
// requirement is a raw audit-log view (M1's Definition of Done); the
// framework/runtime shape (e.g. whether it's a second Next.js app) is
// deferred to whichever milestone first builds a real page here, not
// decided by this scaffold.

export {};
