{
  "name": "@platform/logger",
  "version": "0.0.0",
  "private": true,
  "description": "Structured logging shared by every app and worker. Wraps pino so the rest of the codebase depends on this package's interface, not the underlying library.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "pino": "^10.3.1"
  },
  "devDependencies": {}
}
