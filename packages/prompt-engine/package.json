{
  "name": "@platform/prompt-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Placeholder package created during project scaffolding (M0). Implementation begins in the milestone that owns this package.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "devDependencies": {}
}
