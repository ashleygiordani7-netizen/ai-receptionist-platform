/**
 * Shared Tailwind preset (design tokens: spacing, typography, color).
 *
 * Placeholder created during project scaffolding (M1). Real design tokens
 * are added once @platform/ui begins implementing actual components,
 * per the Design System Principles (design tokens instead of hardcoded
 * values, consistent spacing/typography/color across the whole app).
 */
module.exports = {
  theme: {
    extend: {},
  },
  plugins: [],
};
