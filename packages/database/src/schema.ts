// Drizzle schema definitions for @platform/database.
//
// Placeholder created during project scaffolding (M0). No domain tables
// exist yet — the schema is introduced incrementally, milestone by
// milestone, per the Milestone Roadmap (starting with M1's `organizations`,
// `users`, `organization_members`, `plans`, and `audit_logs`).
//
// This file is `drizzle-kit`'s diff target (see drizzle.config.ts's `schema`
// option): every schema change is expressed here, then
// `pnpm db:generate` produces the corresponding migration SQL.

export {};
